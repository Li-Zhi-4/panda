<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
</head>
<body>
    <h1><%= title %></h1>
    <ul>
        <% if (locals.letters) { %>
            <% letters.forEach( (letter) => { %>
                <li>
                    <strong>Title:</strong> <%= letter.title %> <br>
                    <strong>Content:</strong> <%- letter.content %>
                    <a href="/letters/<%= letter.id %>/edit">Update</a>
                </li>
             <% }); %>
        <% } %>  
    </ul>
    <a href="/letters/new">Create new letter</a>
</body>
</html>